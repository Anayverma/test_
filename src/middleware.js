import { NextResponse } from "next/server";
export function middleware(request) {
  //   if (request.nextUrl.pathname != "/api2/2")
  //     return NextResponse.redirect(new URL("/api2/2", request.url));
}

// export const config = {
//   matcher: ["/test/:path*", "/format/:path*"],
// };
